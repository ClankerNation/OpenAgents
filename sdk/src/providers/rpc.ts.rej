diff a/sdk/src/providers/rpc.ts b/sdk/src/providers/rpc.ts	(rejected hunks)
@@ -1,5 +1,12 @@
 import { withRetry, RetryOptions } from "../utils/retry";
 
+export class RpcError extends Error {
+  constructor(public code: number, message: string) {
+    super(message);
+    this.name = "RpcError";
+  }
+}
+
 export interface JsonRpcRequest {
   jsonrpc: "2.0";
   id: number;
@@ -56,7 +63,7 @@ export class RpcProvider {
       // BUG: Error response is not type-checked — json.error could have unexpected
       // shape and json.result is returned even when error is present
       if (json.error) {
-        throw new Error(`RPC error ${json.error.code}: ${json.error.message}`);
+        throw new RpcError(json.error.code, json.error.message);
       }
 
       return json.result;
